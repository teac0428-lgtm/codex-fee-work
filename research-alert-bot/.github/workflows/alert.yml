name: Research Alert Bot

on:
  workflow_dispatch:
  # TODO: Add a schedule trigger when the bot is ready.

jobs:
  alert:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: TODO run alert bot
        run: |
          echo "TODO: install dependencies and run collector/notification scripts."
